# 项目复利与踩坑日志

## 2026-07-08

- 项目定位：Project 004 选择 `QuoteCash`，一个自由职业者报价单 / 发票 / 收款文案生成器，优先做成无需后端的静态工具。
- 实现取舍：第一版只做本地编辑、计算、打印、导入导出与收款链接承载，不接真实支付 API；真实收款通过用户自己的 Stripe Payment Links、Gumroad、微信/支付宝链接解决。
- 注意事项：`localStorage` 在直接用 `file://` 打开时不同浏览器行为可能不完全一致；正式发布建议使用 GitHub Pages 或其他 HTTP/HTTPS 静态托管。

- 发布状态：已初始化独立 Git 仓库、提交 `main` 分支、加入 GitHub Pages Actions 工作流，并生成 `outputs/quotecash-project004-release.zip`。当前机器没有 `gh`、没有 GitHub token、Chrome 登录通道不可用，远程创建仓库/推送仍需要 GitHub 账号授权或提供远程仓库地址。

- 发布完成：远程仓库 `https://github.com/venus-zhu/quotecash-project004` 已推送 `main` 与 `gh-pages` 分支；线上地址 `https://venus-zhu.github.io/quotecash-project004/` 已验证返回 200，页面标题为 QuoteCash。

- 发布方式调整：移除失败的 GitHub Actions Pages 工作流，最终采用 `gh-pages` 分支发布静态站点；后续更新需要同步推送 `src/` 内容到 `gh-pages`。

- 变现漏斗：新增 `src/config.js`、`src/offer.html`、`src/analytics.js` 和 `docs/monetization.md`；当前默认付款链接为 mailto，占位用，替换成 Stripe/Gumroad/微信等真实收款链接后购买按钮即可转为收款入口。

- Pro 交付包：生成本地付费交付物 `outputs/quotecash-pro-template-pack.zip`，包含 4 套行业 JSON 模板与上架文案；完整包已加入 `.gitignore`，公开站点仅提供 `src/templates/free-design-sample.json` 作为免费样例。

- 推送阻塞：`Add sellable template pack assets` 和 `Add free template sample` 已在本地提交，但 GitHub Git HTTPS 连续无法连接 `github.com:443`；网页可访问，说明是 Git 远程链路间歇性问题。恢复后推送 main 与 gh-pages 即可上线免费样例和 Pro 交付说明。

- 转化优化：主页新增一键载入免费样例模板，销售页新增购买 FAQ 与交付说明；继续保持“公开站点只放免费样例、完整 Pro 包只通过付款平台交付”的隔离策略。
- 付款成功承接页：新增 src/thanks.html 与 src/thanks.js，用于 Gumroad/Stripe 等付款平台成功后跳转；页面只做交付说明和定制服务二次转化，不公开完整 Pro 包。
- 上架信任与 SEO：新增 `src/policies.html`、`robots.txt`、`sitemap.xml` 和社交分享图；主页与销售页加入 canonical/Open Graph，付款成功页设为 noindex。数字商品政策页明确本地数据处理、交付、退款边界和使用许可。
